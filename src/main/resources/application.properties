# mysql 설정
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/MIRACLEDB?serverTimezone=Asia/Seoul&characterEncoding=UTF-8

# 로그인
spring.datasource.username=miracle_admin
spring.datasource.password=0000

# hibernate 설정
# 설정이 바뀌면 테이블 업데이트
# 배포 전 변경 필요
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 파일 업로드 크기 제한
spring.servlet.multipart.maxFileSize=3MB
spring.servlet.multipart.maxRequestSize=3MB

# jwt 키
jwt.secret=MakeYourAwesomeMiracleWithMiracleFarm
jwt.issuer=MiracleFarmOwner